<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Daftar User</title>
    <link rel="stylesheet" href="../assets/style.css">
  </head>
  <body>

    <%- include('../components/navbar')%>
    <h1>User List</h1>
    <p>User yang terdaftar = <%= users.length%> </>
    <ul>
      <% users.forEach((user)=>{%> 
         <li> <a href="/users/<%= user.id %>">  <%= user.name.toUpperCase() %> , <%= user.email %></a></li> 
      <%});
      %>
    </ul>
  </body>
</html>
