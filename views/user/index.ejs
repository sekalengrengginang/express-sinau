<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="assets/style.css">
  </head>
  <body>
    <%- include('../components/navbar')%>
    <h1>User List</h1>

    <ul>
      <% users.forEach((user)=>{%> 
         <li><%= user.id %>. <%= user.name.toUpperCase() %> , <%= user.email %></li> 
      <%});
      %>
    </ul>
  </body>
</html>
